<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-4bw+/aepP/YC94hEpVNVgiZdgIC5+VKNBQNGCHeKRQN+PtmoHDEXuppvnDJzQIu9" crossorigin="anonymous">
    <title>BLOG</title>
</head>
<body style=" background-color: blanchedalmond">
    <div class="container">
        <h1 class="mb-4">Blog Articles</h1>
        <a href="/articles/new" class="btn btn-success">New Article</a>
    </div>

    <div class="container mt-5 mb-4 border bg-light p-4 " >
        <% articles.forEach(article =>{ %>
            <!--<div class="card mx-2 mt-4" style="width: 30%;">-->
                <div class="row g-5 justify-content-evenly">
                    <div class="col-lg-6">
                        <div class="card mt-3">
                            <div class="row g-0">
                                <div class="col-6 col-md-5">
                                    <img class="card-img img-fluid rounded-start" src="<%= article.image[0] %>" alt="Card image cap">
                                </div>
                                <div class="col-6 col-md-7">               
                                    <div class="card-body">
                                        <h4 class="card-title"><%=article.title%></h4>
                                        <div class="card-subtitle text-muted mb-2">
                                            <%= article.createdAt.toLocaleDateString() %>
                                        </div>
                                        <div class="card-text mb-2"><i>Author: <%= article.author %></i></div>
                                        <div class="d-flex justify-content-between">
                                            <a href = "articles/<%= article.slug %>" class="btn btn-primary">Read Blog</a>
                                            <a href="articles/edit/<%= article.id %>" class="btn btn-info">Edit</a>
                                            <form action="/articles/<%= article.id%>?_method=DELETE" method="POST" class="d-inline">
                                            <button type="submit" class="btn btn-danger">Delete</button>
                                            </form>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
        <% }) %>
    </div>
</body>
</html>